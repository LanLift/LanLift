# LanLift 遠端連線設計建議

## 先說結論

LanLift 應提供三種清楚區隔的傳輸方式：**直接連線**、**預設遠端中繼**、與**自訂伺服器**。直接連線應是同一區域網路的預設；使用者跨網路時，程式先嘗試加密點對點連線，再只在無法穿透 NAT／防火牆時將資料導向中繼。GitHub 可用於託管中繼伺服器的原始碼、版本與部署設定，但**不能本身充當一個持續運行的即時中繼伺服器**。

> 遠端模式必須有一台長時間在線、可被兩端連到的服務。GitHub repository、GitHub Pages 與一次性的 CI 工作不具備這個資料中繼角色。

## 使用者會看見的三種模式

| 模式 | 介面文案與流程 | 資料路徑 | 優點 | 邊界與成本 |
|---|---|---|---|---|
| **直接連線** | 「同一個網路」；附近裝置出現後直接傳送。 | Windows 與 Apple 裝置直接在 LAN 串流。 | 最快、零雲端、無伺服器費用。 | 僅同一可互通的區網；訪客 Wi‑Fi／AP isolation 不可用。 |
| **預設遠端連線** | 「遠端」；輸入或選擇收件人的一次性代碼，程式自動建立連線。 | 先嘗試 WebRTC 直接傳輸；失敗時通過 LanLift 的受管 TURN 中繼。 | 對一般使用者最簡單；無需填伺服器。 | 必須營運預設的 LanLift 中繼服務；當資料走 TURN 時會產生伺服器頻寬成本。 |
| **自訂伺服器** | 「自訂伺服器」；輸入或匯入伺服器設定檔。 | 使用者自有的訊號服務與 coturn；仍會優先直連。 | 使用者掌控網域、日誌與頻寬；適合公司、NAS 與高隱私場景。 | 需要使用者或管理員部署與維護伺服器。 |

## 推薦架構

### 1. 直接連線

保留目前 LAN HTTP 串流作為第一階段實作，但改以 mDNS／Bonjour 自動發現代替每次掃 QR Code。已配對裝置採長期公開金鑰識別；傳送方在「附近裝置」清單選擇收件者，資料不離開區域網路。

### 2. 預設遠端連線

應將預設遠端服務命名為 **LanLift Relay**，而非「GitHub 伺服器」。服務由三個小元件組成：

| 元件 | 功能 | 是否承載檔案內容 |
|---|---|---:|
| HTTPS/WSS 訊號服務 | 對一次性代碼進行配對，交換加密協商所需的連線資訊；不保留檔案。 | 否 |
| STUN | 協助兩端找出可直接連線的網路路徑。 | 否 |
| TURN（coturn） | 只在點對點無法建立時，作為加密資料流的備援中繼。 | 是，僅暫時轉送 |

WebRTC 官方文件說明，跨網路時直接 socket 經常不可行，TURN 是必要的備援流量中繼；客戶端應提供中繼的網址、使用者名稱與短期憑證。[1] coturn 是開源的 STUN/TURN 實作，支援 UDP、TCP、TLS、DTLS 及時間限制的 TURN REST API 驗證，可作為自架中繼的基礎。[2]

**安全原則**是：配對代碼僅用於建立工作階段；實際檔案分段在應用層以首次配對保存的裝置金鑰加密，並以工作階段金鑰再次加密。如此即使備援中繼看得到加密流量，也不應能讀取檔案內容或檔名。遠端大檔會受到兩端上行頻寬影響；若落入 TURN，中繼伺服器承擔「上傳一次、下載一次」的雙向流量。

### 3. 自訂伺服器

建議提供一個可匯入／匯出的 JSON 設定檔，至少包含：

```json
{
  "signalingUrl": "wss://relay.example.com",
  "turnUrls": ["turns:relay.example.com:5349?transport=tcp"],
  "turnCredentialMode": "time-limited",
  "serverPublicKey": "base64-public-key"
}
```

進階使用者可在自己的 VPS 以 Docker Compose 運行「訊號服務 + coturn」，再把設定檔匯入 LanLift。部署範本與 Compose 檔案可以存放於 GitHub，但服務本身要部署到使用者的 VPS、NAS 或受管雲端。

另一個適合技術團隊的選擇是 Headscale：它是自架的 Tailscale 控制伺服器，官方定位為個人或小型組織的單一私有網路。[3] 這讓使用者先將裝置放入私有 mesh 網路，再由 LanLift 以私有 IP 直連。然而它需要額外安裝並管理 VPN 客戶端，並不適合作為一般使用者的預設遠端模式。

## 不建議的做法

| 做法 | 不建議原因 |
|---|---|
| 將 GitHub Pages 當作遠端中繼 | 它只提供靜態內容，沒有常駐 socket 或檔案轉送能力。 |
| 將 GitHub Actions 當作中繼 | 工作不是長駐連線服務，且不適合承接私人檔案流量。 |
| 一開始就將檔案上傳至一般資料庫或 GitHub Releases | 無法滿足即時傳輸、隱私、刪除時效與頻寬成本控制。 |
| 不經同意永久保存遠端檔案 | 增加敏感資料、合規、儲存成本與刪除責任。 |

## 推薦的產品決策

第一版遠端功能應選擇**「直接優先，受管／自訂 TURN 備援」**。這保有相同網路的最高速度，也讓跨網路情況能夠連上。第一個可交付版本應先完成「自訂伺服器」設定，讓你可控制資料路徑與成本；當你決定要營運服務和設定頻寬／帳號政策後，再啟用「預設遠端連線」。

## 參考資料

[1] [WebRTC：TURN server](https://webrtc.org/getting-started/turn-server)

[2] [coturn：開源 TURN/STUN Server](https://github.com/coturn/coturn)

[3] [Headscale：自架 Tailscale 控制伺服器](https://headscale.net/stable/)

---

作者：Manus AI  
日期：2026-08-18
